#include "./parser.h"

int main()
{
    // char *program = "add r1, 223 + (34 * 5)";
    // Lexer lex = lexer(sv_from_cstr(program));

    // print_lex(&lex);
    printf("sizeof(CPU)=%zu\n",sizeof(CPU));
    return 0;
}